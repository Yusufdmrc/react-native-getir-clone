import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  categories: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    flexWrap: "wrap",
  },
});

export default styles;
